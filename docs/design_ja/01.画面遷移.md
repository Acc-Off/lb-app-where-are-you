# 画面遷移図

## 画面一覧

| 画面ID | 画面名                     | 種類               | 説明                                               |
| ------ | -------------------------- | ------------------ | -------------------------------------------------- |
| S01    | 初回同意画面               | overlay            | 初回起動時のみ表示。位置共有の説明と同意           |
| S02    | マップ画面（メイン）       | tab                | フレンドの位置をマップ上に CircleMarker で表示     |
| S03    | ゴーストモード設定パネル   | modal-card         | 全体切替 + フレンド/グループ別設定を1シートで操作  |
| S04    | フレンド一覧画面           | tab                | コンパクト1行レイアウトでステータス+アクション     |
| S05    | フレンド追加画面           | modal-card         | 近くのプレーヤー候補またはID/名前検索で申請        |
| S06    | フレンド詳細ダイアログ     | `<dialog>`         | 対象の1行サマリと関係表示（フレンド/同一グループ） |
| S07    | ブロック確認ダイアログ     | `<dialog>`         | ブロック実行前の確認。Esc/backdrop で閉じ可        |
| S07b   | フレンド削除確認ダイアログ | `<dialog>`         | 削除実行前の確認。Esc/backdrop で閉じ可            |
| S08    | タイムライン画面           | tab                | フレンドのチェックイン投稿一覧                     |
| S10    | リアクション送信モーダル   | modal              | スタンプ・短文を送れるモーダル                     |
| S11    | 設定画面                   | tab                | 通知・プライバシー・更新間隔設定                   |
| S12    | グループ画面               | tab内サブ or modal | グループ作成・参加・退出・メンバー一覧             |
| S16    | 汎用削除確認ダイアログ     | `<dialog>`         | 投稿削除/リアクション削除/退会を確認               |

---

## アプリ起動フロー

```mermaid
flowchart TD
    START([アプリ起動]) --> FIRST{初回起動?}
    FIRST -->|Yes| S01[S01 初回同意画面]
    FIRST -->|No| S02
    S01 -->|同意する| S02[S02 マップ画面]
```

---

## メイン遷移

```mermaid
flowchart TD
    S02[S02 マップ画面]

    S02 -->|status-pill タップ| S03[S03 ゴーストモード<br>modal-card]
    S03 -->|overlay閉じ / モード選択| S02

    S02 -->|マーカータップ → 詳細| S06[S06 フレンド詳細<br>dialog]
    S06 -->|ブロック| S07[S07 ブロック確認<br>dialog]
    S06 -->|フレンド削除| S07B[S07b 削除確認<br>dialog]
    S06 -->|閉じる / Esc / backdrop| S02
    S07 -->|ブロックする| S04[S04 フレンド一覧]
    S07 -->|キャンセル / Esc / backdrop| CLOSE_CONFIRM[ダイアログ閉じ]
    S07B -->|削除する| S04
    S07B -->|キャンセル / Esc / backdrop| CLOSE_CONFIRM

    S02 -->|タブ: 友達| S04
    S04 -->|+追加| S05[S05 フレンド追加<br>modal-card]
    S04 -->|詳細ボタン| S06
    S05 -->|overlay閉じ| S04
```

注: S06 → S07/S07b は S06 を close してから S07/S07b を showModal する流れ。
ブロック/削除実行後は友達一覧（S04）に遷移してリフレッシュ。

---

## タブナビゲーション

```mermaid
flowchart LR
    TAB_MAP[🗺 地図<br>S02]
    TAB_TL[📝 タイムライン<br>S08]
    TAB_FR[👥 友達<br>S04]
    TAB_GR[📻 グループ<br>S12]
    TAB_ST[⚙ 設定<br>S11]

    TAB_MAP <-->|タブ切替| TAB_TL
    TAB_MAP <-->|タブ切替| TAB_FR
    TAB_MAP <-->|タブ切替| TAB_GR
    TAB_MAP <-->|タブ切替| TAB_ST
```

---

## タイムライン・グループ・フィルタ 遷移

```mermaid
flowchart TD
    S08[S08 タイムライン] -->|リアクションをタップ| S10[S10 リアクション<br>送信モーダル]
    S08 -->|チェックイン投稿| S08
    S08 -->|📍 地図ジャンプ| S02
    S08 -->|🗑️ 投稿/リアクション削除| S16[S16 削除確認<br>dialog]
    S10 -->|送信 / キャンセル| S08
    S16 -->|OK / キャンセル| S08

    S02 -->|ビューフィルタ| VF[ビューフィルタ<br>dropdown]
    VF -->|選択| S02

    S11[S11 設定画面] -->|グループ| S12[S12 グループ画面]
    S11 -->|退会（全データ削除）| S16
    S12 -->|＋追加| S12A[S12a グループ参加/作成<br>モーダル]
    S12A -->|参加/作成 or キャンセル| S12
    S12 -->|退出| S12
```

---

## 遷移サマリー（全体）

```mermaid
flowchart TD
    S01 --> S02
    S02 <-->|overlay| S03
    S02 -->|dialog| S06
    S06 -->|dialog| S07
    S06 -->|dialog| S07b
    S07 --> S04
    S07b --> S04
    S02 -->|フィルタ| VF[ビューフィルタ]
    S02 <-->|tab| S04
    S02 <-->|tab| S08
    S02 <-->|tab| S12
    S02 <-->|tab| S11
    S04 -->|overlay| S05
    S04 -->|dialog| S06
    S08 --> S10
    S08 --> S16
    S11 --> S16
    S11 --> S12
    S12 --> S12a
```
